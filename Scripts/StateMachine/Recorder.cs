using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Recorder : MonoBehaviour
{

    public bool record_position = false;
    public bool record_rotation = false;


    // Update is called once per frame
    void Update()
    {
        if(record_position){
            //record position
        }
        if(record_rotation){
            //record rotation
        }
    }
}
